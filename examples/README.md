# examples/

A fictional worked example, so the repo is not an empty promise.

`Worked Example.md` shows what one role assessment looks like after `/career-init` and a few rounds of
`/interview` — the frontmatter, the trust fields, and the kind of reasoning the framework is meant to
produce. **The person and the employer are invented.** Nobody's real career is in this repo.
